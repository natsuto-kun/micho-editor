# M0 骨格 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Wails v2 + React + TypeScript + CodeMirror 6 + SQLite を組み合わせた空のエディタを起動し、文字が打てる状態にする。

**Architecture:** Go（Wails v2）がバックエンド、React + TypeScript がフロントエンド、SQLite（modernc.org/sqlite）がデータ永続化層。フロントエンドでは CodeMirror 6 を最小構成でマウントし、SQLite は起動時にマイグレーションを自動適用する。

**Tech Stack:** Wails v2, Go 1.26.5, modernc.org/sqlite, React 18, TypeScript, CodeMirror 6, Vite, zustand

---

## ファイル構成

**新規作成:**
- `main.go` — Wails エントリポイント
- `app.go` — フロントに公開する API（M0 では空）
- `wails.json` — Wails 設定ファイル
- `go.mod` / `go.sum` — Go モジュール
- `internal/store/db.go` — SQLite 接続・PRAGMA・マイグレーションランナー
- `internal/store/migrations/0001_init.sql` — scenarios / sections / media / snapshots / links テーブル
- `internal/store/migrations/0002_fts.sql` — FTS5 仮想テーブル + trigger 3本
- `frontend/` — Wails が生成する React + TypeScript + Vite 構成
- `frontend/src/editor/setup.ts` — 最小構成の CM6 EditorState / EditorView
- `frontend/src/App.tsx` — CM6 を React コンポーネントとしてマウント（Wails 生成ファイルを上書き）

**変更:**
- `frontend/package.json` — CM6 / lezer / zustand パッケージ追加

---

## Task 1: Wails CLI のインストール

**Files:**
- なし（グローバルツールのインストール）

- [ ] **Step 1: wails CLI をインストール**

```bash
go install github.com/wailsapp/wails/v2/cmd/wails@latest
```

- [ ] **Step 2: PATH を確認してインストール成功を検証**

```bash
export PATH="$PATH:$(go env GOPATH)/bin"
wails version
```

Expected: `Wails CLI v2.x.x` が表示される

---

## Task 2: Wails プロジェクトの初期化

**Background:** `wails init -n <name>` は現在のディレクトリに `<name>/` サブディレクトリを作成する。既存の git リポジトリ（micho-edit）に直接展開するため、親ディレクトリで init し、生成物を移動する。

**Files:**
- Create: `main.go`
- Create: `app.go`
- Create: `wails.json`
- Create: `go.mod`
- Create: `frontend/` (全体)

- [ ] **Step 1: 親ディレクトリで wails init を実行**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool
export PATH="$PATH:$(go env GOPATH)/bin"
wails init -n trpg-editor -t react-ts
```

Expected: `/Users/nakamuranatsu/Desktop/develop/tool/trpg-editor/` が生成される

- [ ] **Step 2: 生成されたファイルを micho-edit に移動**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool
# 生成物をすべて micho-edit へ移動（git 管理ファイルは除く）
cp -r trpg-editor/frontend micho-edit/
cp trpg-editor/main.go trpg-editor/app.go trpg-editor/go.mod trpg-editor/go.sum trpg-editor/wails.json micho-edit/
rm -rf trpg-editor
```

- [ ] **Step 3: go.mod のモジュール名を確認・修正**

`/Users/nakamuranatsu/Desktop/develop/tool/micho-edit/go.mod` を開いて、モジュール名が `trpg-editor` になっていることを確認。必要なら `micho-edit` に変更：

```bash
# go.mod の module 行を確認
head -3 /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/go.mod
```

`module trpg-editor` のままで問題なし（内部名なので）

- [ ] **Step 4: ディレクトリ構造の確認**

```bash
ls /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/
```

Expected: `main.go`, `app.go`, `go.mod`, `go.sum`, `wails.json`, `frontend/`, `docs/`, `CLAUDE.md`, `STEP.md` が存在する

---

## Task 3: Go 依存パッケージの追加

**Files:**
- Modify: `go.mod`, `go.sum`

- [ ] **Step 1: modernc.org/sqlite と uuid を追加**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
go get modernc.org/sqlite
go get github.com/google/uuid
```

- [ ] **Step 2: goldmark を追加（M3 で使うが go.mod に記録しておく）**

```bash
go get github.com/yuin/goldmark
```

- [ ] **Step 3: 依存が正しく追加されたことを確認**

```bash
grep -E "modernc|uuid|goldmark" go.mod
```

Expected:
```
github.com/google/uuid v...
github.com/yuin/goldmark v...
modernc.org/sqlite v...
```

---

## Task 4: SQLite マイグレーションファイルの作成

**Files:**
- Create: `internal/store/migrations/0001_init.sql`
- Create: `internal/store/migrations/0002_fts.sql`

- [ ] **Step 1: ディレクトリ作成**

```bash
mkdir -p /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/internal/store/migrations
```

- [ ] **Step 2: 0001_init.sql を作成**

`internal/store/migrations/0001_init.sql`:

```sql
CREATE TABLE scenarios (
  id          TEXT PRIMARY KEY,
  title       TEXT NOT NULL,
  system      TEXT,
  players     TEXT,
  play_time   TEXT,
  meta        TEXT NOT NULL DEFAULT '{}',
  created_at  INTEGER NOT NULL,
  updated_at  INTEGER NOT NULL
);

CREATE TABLE sections (
  id          TEXT PRIMARY KEY,
  scenario_id TEXT NOT NULL REFERENCES scenarios(id) ON DELETE CASCADE,
  parent_id   TEXT REFERENCES sections(id) ON DELETE CASCADE,
  kind        TEXT NOT NULL,
  title       TEXT NOT NULL,
  body        TEXT NOT NULL DEFAULT '',
  sort_key    TEXT NOT NULL,
  rev         INTEGER NOT NULL DEFAULT 1,
  updated_at  INTEGER NOT NULL
);

CREATE INDEX idx_sections_scenario ON sections(scenario_id, sort_key);
CREATE INDEX idx_sections_parent   ON sections(parent_id, sort_key);

CREATE TABLE media (
  id          TEXT PRIMARY KEY,
  scenario_id TEXT NOT NULL REFERENCES scenarios(id) ON DELETE CASCADE,
  filename    TEXT NOT NULL,
  mime        TEXT NOT NULL,
  width       INTEGER,
  height      INTEGER,
  bytes       INTEGER NOT NULL,
  rel_path    TEXT NOT NULL,
  created_at  INTEGER NOT NULL
);

CREATE TABLE snapshots (
  id          TEXT PRIMARY KEY,
  section_id  TEXT NOT NULL REFERENCES sections(id) ON DELETE CASCADE,
  rev         INTEGER NOT NULL,
  body_gz     BLOB NOT NULL,
  created_at  INTEGER NOT NULL
);

CREATE INDEX idx_snapshots_section ON snapshots(section_id, created_at DESC);

CREATE TABLE links (
  from_id     TEXT NOT NULL REFERENCES sections(id) ON DELETE CASCADE,
  to_id       TEXT NOT NULL REFERENCES sections(id) ON DELETE CASCADE,
  PRIMARY KEY (from_id, to_id)
);
```

- [ ] **Step 3: 0002_fts.sql を作成**

`internal/store/migrations/0002_fts.sql`:

```sql
CREATE VIRTUAL TABLE section_fts USING fts5(
  title,
  body,
  content='sections',
  content_rowid='rowid',
  tokenize='trigram'
);

CREATE TRIGGER sections_ai AFTER INSERT ON sections BEGIN
  INSERT INTO section_fts(rowid, title, body)
    VALUES (new.rowid, new.title, new.body);
END;

CREATE TRIGGER sections_ad AFTER DELETE ON sections BEGIN
  INSERT INTO section_fts(section_fts, rowid, title, body)
    VALUES('delete', old.rowid, old.title, old.body);
END;

CREATE TRIGGER sections_au AFTER UPDATE ON sections BEGIN
  INSERT INTO section_fts(section_fts, rowid, title, body)
    VALUES('delete', old.rowid, old.title, old.body);
  INSERT INTO section_fts(rowid, title, body)
    VALUES (new.rowid, new.title, new.body);
END;
```

---

## Task 5: SQLite 接続・マイグレーションランナーの実装

**Files:**
- Create: `internal/store/db.go`

- [ ] **Step 1: db.go を作成**

`internal/store/db.go`:

```go
package store

import (
	"database/sql"
	"embed"
	"fmt"
	"io/fs"
	"path/filepath"
	"sort"
	"strings"

	_ "modernc.org/sqlite"
)

//go:embed migrations/*.sql
var migrationsFS embed.FS

type Store struct {
	db *sql.DB
}

func Open(path string) (*Store, error) {
	db, err := sql.Open("sqlite", path+"?_pragma=journal_mode(WAL)&_pragma=synchronous(NORMAL)&_pragma=foreign_keys(ON)")
	if err != nil {
		return nil, fmt.Errorf("open db: %w", err)
	}
	if err := db.Ping(); err != nil {
		return nil, fmt.Errorf("ping db: %w", err)
	}

	s := &Store{db: db}
	if err := s.migrate(); err != nil {
		db.Close()
		return nil, fmt.Errorf("migrate: %w", err)
	}
	return s, nil
}

func (s *Store) Close() error {
	return s.db.Close()
}

func (s *Store) DB() *sql.DB {
	return s.db
}

func (s *Store) migrate() error {
	if _, err := s.db.Exec(`CREATE TABLE IF NOT EXISTS schema_migrations (
		version TEXT PRIMARY KEY,
		applied_at INTEGER NOT NULL
	)`); err != nil {
		return fmt.Errorf("create schema_migrations: %w", err)
	}

	files, err := fs.Glob(migrationsFS, "migrations/*.sql")
	if err != nil {
		return err
	}
	sort.Strings(files)

	for _, file := range files {
		version := strings.TrimSuffix(filepath.Base(file), ".sql")

		var exists bool
		err := s.db.QueryRow(`SELECT EXISTS(SELECT 1 FROM schema_migrations WHERE version = ?)`, version).Scan(&exists)
		if err != nil {
			return fmt.Errorf("check migration %s: %w", version, err)
		}
		if exists {
			continue
		}

		sql, err := fs.ReadFile(migrationsFS, file)
		if err != nil {
			return fmt.Errorf("read migration %s: %w", file, err)
		}

		if _, err := s.db.Exec(string(sql)); err != nil {
			return fmt.Errorf("apply migration %s: %w", version, err)
		}

		if _, err := s.db.Exec(
			`INSERT INTO schema_migrations(version, applied_at) VALUES (?, strftime('%s','now'))`,
			version,
		); err != nil {
			return fmt.Errorf("record migration %s: %w", version, err)
		}
	}
	return nil
}
```

- [ ] **Step 2: Go のビルドを確認（store パッケージ単体）**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
go build ./internal/store/...
```

Expected: エラーなし（警告は問題なし）

---

## Task 6: app.go と main.go の更新

M0 では `app.go` にビジネスロジックはなく、Store の初期化のみ行う。`main.go` は Wails が生成したままで問題ないが、Store 初期化を追加する。

**Files:**
- Modify: `app.go`
- Modify: `main.go`

- [ ] **Step 1: app.go の現在の内容を確認**

```bash
cat /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/app.go
```

- [ ] **Step 2: app.go を書き換え**

`app.go` を以下の内容に差し替え（Wails が生成した内容を上書き）：

```go
package main

import (
	"context"
	"path/filepath"

	"github.com/wailsapp/wails/v2/pkg/runtime"
	"trpg-editor/internal/store"
)

type App struct {
	ctx   context.Context
	store *store.Store
}

func NewApp() *App {
	return &App{}
}

func (a *App) startup(ctx context.Context) {
	a.ctx = ctx

	home, _ := runtime.HomeDir(ctx)
	dbPath := filepath.Join(home, ".trpg-editor", "default.trpg")

	s, err := store.Open(dbPath)
	if err != nil {
		runtime.LogErrorf(ctx, "Failed to open store: %v", err)
		return
	}
	a.store = s
}

func (a *App) shutdown(ctx context.Context) {
	if a.store != nil {
		a.store.Close()
	}
}

func (a *App) Store() *store.Store {
	return a.store
}
```

**Note:** `trpg-editor/internal/store` の `trpg-editor` 部分は go.mod の module 名に合わせる。`go.mod` の先頭行が `module trpg-editor` なら上記のまま。

- [ ] **Step 3: main.go の現在の内容を確認**

```bash
cat /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/main.go
```

- [ ] **Step 4: main.go に shutdown フックを追加**

Wails 生成の `main.go` には `OnShutdown` が含まれていないことが多い。以下のように `OnShutdown: app.shutdown` を追加する（`wails.Run` の options に追記）：

```go
// main.go 内の wails.Run(&options.App{...}) に追加
OnShutdown: app.shutdown,
```

- [ ] **Step 5: ビルド確認**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
go build ./...
```

Expected: コンパイルエラーなし

---

## Task 7: フロントエンド依存パッケージの追加

**Files:**
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json` or `frontend/node_modules/`

- [ ] **Step 1: 現在の package.json を確認**

```bash
cat /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend/package.json
```

- [ ] **Step 2: CM6 / lezer / zustand パッケージをインストール**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npm install \
  @codemirror/state \
  @codemirror/view \
  @codemirror/commands \
  @codemirror/language \
  @codemirror/lang-markdown \
  @codemirror/autocomplete \
  @codemirror/search \
  @lezer/markdown \
  @lezer/highlight \
  zustand
```

- [ ] **Step 3: CM6 の重複インストールがないことを確認**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npm ls @codemirror/state
```

Expected: `@codemirror/state` が1つだけリストされる（重複なし）

---

## Task 8: 最小構成の CM6 セットアップ

M0 では独自拡張（markdown-ext, live-preview, autosave 等）は一切実装しない。基本的な EditorView が表示・入力できれば完了。

**Files:**
- Create: `frontend/src/editor/setup.ts`

- [ ] **Step 1: editor ディレクトリを作成**

```bash
mkdir -p /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend/src/editor
```

- [ ] **Step 2: setup.ts を作成**

`frontend/src/editor/setup.ts`:

```typescript
import { EditorState } from "@codemirror/state";
import {
  EditorView,
  keymap,
  highlightActiveLine,
  drawSelection,
  lineNumbers,
} from "@codemirror/view";
import {
  history,
  historyKeymap,
  defaultKeymap,
} from "@codemirror/commands";
import { markdown, markdownLanguage } from "@codemirror/lang-markdown";
import { syntaxHighlighting, defaultHighlightStyle } from "@codemirror/language";
import { search, searchKeymap } from "@codemirror/search";

export function createEditorState(doc: string): EditorState {
  return EditorState.create({
    doc,
    extensions: [
      EditorView.lineWrapping,
      lineNumbers(),
      history({ minDepth: 50, newGroupDelay: 300 }),
      drawSelection(),
      highlightActiveLine(),
      search({ top: true }),
      keymap.of([...defaultKeymap, ...historyKeymap, ...searchKeymap]),
      markdown({ base: markdownLanguage }),
      syntaxHighlighting(defaultHighlightStyle),
    ],
  });
}

export function createEditorView(
  parent: HTMLElement,
  doc: string
): EditorView {
  return new EditorView({
    state: createEditorState(doc),
    parent,
  });
}
```

---

## Task 9: App.tsx を CM6 エディタコンポーネントに書き換え

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: 現在の App.tsx を確認**

```bash
cat /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend/src/App.tsx
```

- [ ] **Step 2: App.tsx を書き換え**

`frontend/src/App.tsx`:

```tsx
import { useEffect, useRef } from "react";
import { EditorView } from "@codemirror/view";
import { createEditorView } from "./editor/setup";

const INITIAL_DOC = `# TRPG シナリオエディタ

ここに本文を書きます。
`;

export default function App() {
  const containerRef = useRef<HTMLDivElement>(null);
  const viewRef = useRef<EditorView | null>(null);

  useEffect(() => {
    if (!containerRef.current) return;

    const view = createEditorView(containerRef.current, INITIAL_DOC);
    viewRef.current = view;

    return () => {
      view.destroy();
      viewRef.current = null;
    };
  }, []);

  return (
    <div style={{ height: "100vh", display: "flex", flexDirection: "column" }}>
      <div
        ref={containerRef}
        style={{ flex: 1, overflow: "auto" }}
      />
    </div>
  );
}
```

- [ ] **Step 3: TypeScript 型エラーがないことを確認**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npx tsc --noEmit
```

Expected: エラーなし

---

## Task 10: ~/.trpg-editor ディレクトリの自動作成

`store.Open` が呼ぶ dbPath の親ディレクトリが存在しないと SQLite がエラーになる。`db.go` の `Open` 関数に `os.MkdirAll` を追加する。

**Files:**
- Modify: `internal/store/db.go`

- [ ] **Step 1: db.go の Open 関数を修正**

`internal/store/db.go` の `Open` 関数先頭に追加：

```go
import (
    // 既存の import に追加
    "os"
)

func Open(path string) (*Store, error) {
    // DB ファイルの親ディレクトリを作成
    if err := os.MkdirAll(filepath.Dir(path), 0o700); err != nil {
        return nil, fmt.Errorf("create db dir: %w", err)
    }
    // ... 既存の処理
}
```

- [ ] **Step 2: ビルド確認**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
go build ./...
```

Expected: エラーなし

---

## Task 11: wails dev で動作確認

**Files:** なし（確認作業のみ）

- [ ] **Step 1: wails dev を起動**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
export PATH="$PATH:$(go env GOPATH)/bin"
wails dev
```

Expected:
- コンパイルエラーなし
- ブラウザまたはデスクトップウィンドウが開く
- CM6 エディタが表示される

- [ ] **Step 2: 入力確認**

- アルファベット・数字が入力できることを確認
- バックスペース・矢印キーが正常に動作することを確認
- Ctrl+Z / Cmd+Z でアンドゥできることを確認

- [ ] **Step 3: SQLite 接続確認**

`~/.trpg-editor/default.trpg` が生成されていることを確認：

```bash
ls -la ~/.trpg-editor/
```

Expected: `default.trpg` ファイルが存在する

- [ ] **Step 4: DB スキーマ確認**

```bash
sqlite3 ~/.trpg-editor/default.trpg ".tables"
```

Expected:
```
links            schema_migrations  section_fts      sections
media            scenarios          snapshots
```

- [ ] **Step 5: M0 完了コミット**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
git add .
git commit -m "$(cat <<'EOF'
feat: M0 骨格 — Wails + CM6 + SQLite で空エディタを起動

- wails init (react-ts テンプレート) でプロジェクトを初期化
- modernc.org/sqlite を追加し WAL/外部キー有効で接続
- 0001_init.sql / 0002_fts.sql マイグレーション実装
- 起動時に未適用マイグレーションを自動適用するランナー実装
- CM6 最小構成エディタ (EditorView) を App.tsx にマウント

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review チェック

### Spec coverage
- [x] `wails init` でプロジェクト生成 → Task 2
- [x] `modernc.org/sqlite` を Go モジュールに追加 → Task 3
- [x] `internal/store/db.go` — SQLite 接続・PRAGMA 設定 → Task 5
- [x] `internal/store/migrations/0001_init.sql` — テーブル作成 → Task 4
- [x] `internal/store/migrations/0002_fts.sql` — FTS5 仮想テーブル + trigger → Task 4
- [x] マイグレーションランナー実装 → Task 5
- [x] フロントエンド依存パッケージ追加 → Task 7
- [x] `frontend/src/editor/setup.ts` — CM6 最小構成 → Task 8
- [x] `frontend/src/App.tsx` — CM6 マウント → Task 9
- [x] `wails dev` で空エディタ起動・入力確認 → Task 11

### 注意事項
- `go.mod` のモジュール名 (`trpg-editor`) と `app.go` のインポートパスが一致していることを Task 6 Step 2 で確認すること
- Task 10 の `os.MkdirAll` 追加を忘れると初回起動時に DB 接続エラーになる
- Task 7 後に `npm ls @codemirror/state` で重複インストールを確認すること（CM6 は同一パッケージの複数バージョンでランタイムエラーになる）

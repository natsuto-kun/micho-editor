# M0.5 IME 検証 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `view.composing` ガードを持つ autosave.ts と live-preview.ts のスタブを実装し、wails dev 上でのmacOS 日本語 IME 入力を検証できる状態にする。

**Architecture:** M0.5 は本格実装ではなくゲート検証。`autosave.ts` は SaveSection バインディング未実装のためコンソールログで代替。`live-preview.ts` は装飾なしのスケルトン ViewPlugin として実装し、どちらも `view.composing` チェックだけを確実に入れる。IME デバッグ表示（composing インジケータ）を App.tsx に追加して変換中状態を可視化する。

**Tech Stack:** CodeMirror 6 (ViewPlugin, updateListener), React, TypeScript, Wails v2

---

## ファイル構成

**新規作成:**
- `frontend/src/editor/autosave.ts` — composing ガード付きのスタブオートセーブ（800ms debounce + 5s flush。M0.5 ではコンソールログのみ）
- `frontend/src/editor/live-preview.ts` — composing ガード付きのスケルトン ViewPlugin（M0.5 では装飾なし）

**変更:**
- `frontend/src/editor/setup.ts` — autosave と livePreview を extensions に追加
- `frontend/src/App.tsx` — IME デバッグインジケータ（変換中バッジ）を追加

---

## Task 1: autosave.ts のスタブ実装（composing ガード付き）

**Files:**
- Create: `frontend/src/editor/autosave.ts`

- [ ] **Step 1: autosave.ts を作成**

`frontend/src/editor/autosave.ts`:

```typescript
import { EditorView } from "@codemirror/view";

const IDLE_MS = 800;
const MAX_INTERVAL_MS = 5000;

export function autosave(sectionId: string) {
  let timer: number | undefined;
  let firstDirtyAt = 0;

  const flush = (view: EditorView) => {
    window.clearTimeout(timer);
    timer = undefined;
    firstDirtyAt = 0;
    const body = view.state.doc.toString();
    // M0.5: SaveSection は未実装のためコンソールログで代替
    console.log(`[autosave] section=${sectionId} chars=${body.length}`);
  };

  return EditorView.updateListener.of((u) => {
    if (!u.docChanged) return;
    if (u.view.composing) return; // IME 変換確定前は保存しない

    const now = Date.now();
    if (!firstDirtyAt) firstDirtyAt = now;

    if (now - firstDirtyAt >= MAX_INTERVAL_MS) {
      flush(u.view);
      return;
    }
    window.clearTimeout(timer);
    timer = window.setTimeout(() => flush(u.view), IDLE_MS);
  });
}
```

- [ ] **Step 2: TypeScript 型チェック**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npx tsc --noEmit 2>&1 | grep -v "ExperimentalWarning"
```

期待値: エラーなし

---

## Task 2: live-preview.ts のスケルトン実装（composing ガード付き）

**Files:**
- Create: `frontend/src/editor/live-preview.ts`

- [ ] **Step 1: live-preview.ts を作成**

`frontend/src/editor/live-preview.ts`:

```typescript
import {
  ViewPlugin,
  DecorationSet,
  EditorView,
  ViewUpdate,
  Decoration,
} from "@codemirror/view";

export const livePreview = ViewPlugin.fromClass(
  class {
    decorations: DecorationSet;

    constructor(_view: EditorView) {
      this.decorations = Decoration.none;
    }

    update(u: ViewUpdate) {
      if (u.view.composing) return; // IME 変換中は装飾を再構築しない
      // M0.5: 装飾なし。M3 で :::npc 等のウィジェット実装を追加する
    }
  },
  {
    decorations: (v) => v.decorations,
    provide: (plugin) =>
      EditorView.atomicRanges.of(
        (view) => view.plugin(plugin)?.decorations ?? Decoration.none
      ),
  }
);
```

- [ ] **Step 2: TypeScript 型チェック**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npx tsc --noEmit 2>&1 | grep -v "ExperimentalWarning"
```

期待値: エラーなし

---

## Task 3: setup.ts に autosave と livePreview を組み込む

**Files:**
- Modify: `frontend/src/editor/setup.ts`

現在の `setup.ts` の内容:

```typescript
import { EditorState } from "@codemirror/state";
import {
  EditorView,
  keymap,
  highlightActiveLine,
  drawSelection,
  lineNumbers,
} from "@codemirror/view";
import {
  history,
  historyKeymap,
  defaultKeymap,
} from "@codemirror/commands";
import { markdown, markdownLanguage } from "@codemirror/lang-markdown";
import { syntaxHighlighting, defaultHighlightStyle } from "@codemirror/language";
import { search, searchKeymap } from "@codemirror/search";

export function createEditorState(doc: string): EditorState {
  return EditorState.create({
    doc,
    extensions: [
      EditorView.lineWrapping,
      lineNumbers(),
      history({ minDepth: 50, newGroupDelay: 300 }),
      drawSelection(),
      highlightActiveLine(),
      search({ top: true }),
      keymap.of([...defaultKeymap, ...historyKeymap, ...searchKeymap]),
      markdown({ base: markdownLanguage }),
      syntaxHighlighting(defaultHighlightStyle),
    ],
  });
}

export function createEditorView(
  parent: HTMLElement,
  doc: string
): EditorView {
  return new EditorView({
    state: createEditorState(doc),
    parent,
  });
}
```

- [ ] **Step 1: setup.ts を更新**

`frontend/src/editor/setup.ts` を以下の内容に完全置換:

```typescript
import { EditorState } from "@codemirror/state";
import {
  EditorView,
  keymap,
  highlightActiveLine,
  drawSelection,
  lineNumbers,
} from "@codemirror/view";
import {
  history,
  historyKeymap,
  defaultKeymap,
} from "@codemirror/commands";
import { markdown, markdownLanguage } from "@codemirror/lang-markdown";
import { syntaxHighlighting, defaultHighlightStyle } from "@codemirror/language";
import { search, searchKeymap } from "@codemirror/search";
import { autosave } from "./autosave";
import { livePreview } from "./live-preview";

export function createEditorState(doc: string, sectionId: string): EditorState {
  return EditorState.create({
    doc,
    extensions: [
      EditorView.lineWrapping,
      lineNumbers(),
      history({ minDepth: 50, newGroupDelay: 300 }),
      drawSelection(),
      highlightActiveLine(),
      search({ top: true }),
      keymap.of([...defaultKeymap, ...historyKeymap, ...searchKeymap]),
      markdown({ base: markdownLanguage }),
      syntaxHighlighting(defaultHighlightStyle),
      livePreview,
      autosave(sectionId),
    ],
  });
}

export function createEditorView(
  parent: HTMLElement,
  doc: string,
  sectionId: string
): EditorView {
  return new EditorView({
    state: createEditorState(doc, sectionId),
    parent,
  });
}
```

**注意:** `createEditorState` と `createEditorView` に `sectionId: string` 引数を追加した。

- [ ] **Step 2: App.tsx の createEditorView 呼び出しを更新**

`frontend/src/App.tsx` の `createEditorView` 呼び出しに `sectionId` を追加する。

現在の App.tsx の該当行:
```tsx
const view = createEditorView(containerRef.current, INITIAL_DOC);
```

変更後:
```tsx
const view = createEditorView(containerRef.current, INITIAL_DOC, "m0-debug");
```

- [ ] **Step 3: TypeScript 型チェック**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npx tsc --noEmit 2>&1 | grep -v "ExperimentalWarning"
```

期待値: エラーなし

---

## Task 4: IME デバッグインジケータを App.tsx に追加

composing 状態を React 側で可視化し、ガードが効いていることを目視確認できるようにする。

**Files:**
- Modify: `frontend/src/App.tsx`

- [ ] **Step 1: App.tsx を更新**

`frontend/src/App.tsx` を以下の内容に完全置換:

```tsx
import { useEffect, useRef, useState } from "react";
import { EditorView } from "@codemirror/view";
import { createEditorView } from "./editor/setup";

const INITIAL_DOC = `# TRPG シナリオエディタ

ここに日本語を入力して IME 変換を確認してください。

例: 「にほんご」と入力して漢字に変換してみてください。
`;

export default function App() {
  const containerRef = useRef<HTMLDivElement>(null);
  const viewRef = useRef<EditorView | null>(null);
  const [composing, setComposing] = useState(false);

  useEffect(() => {
    if (!containerRef.current) return;

    const view = createEditorView(containerRef.current, INITIAL_DOC, "m0-debug");
    viewRef.current = view;

    const dom = view.dom;
    const onStart = () => setComposing(true);
    const onEnd = () => setComposing(false);
    dom.addEventListener("compositionstart", onStart);
    dom.addEventListener("compositionend", onEnd);

    return () => {
      dom.removeEventListener("compositionstart", onStart);
      dom.removeEventListener("compositionend", onEnd);
      view.destroy();
      viewRef.current = null;
    };
  }, []);

  return (
    <div style={{ height: "100vh", display: "flex", flexDirection: "column" }}>
      <div
        style={{
          padding: "4px 12px",
          background: composing ? "#f59e0b" : "#1e293b",
          color: composing ? "#000" : "#94a3b8",
          fontSize: "12px",
          fontFamily: "monospace",
          transition: "background 0.1s",
        }}
      >
        {composing ? "⌨ IME 変換中 — composing: true" : "composing: false"}
      </div>
      <div
        ref={containerRef}
        style={{ flex: 1, overflow: "auto" }}
      />
    </div>
  );
}
```

- [ ] **Step 2: TypeScript 型チェック**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npx tsc --noEmit 2>&1 | grep -v "ExperimentalWarning"
```

期待値: エラーなし

- [ ] **Step 3: フロントエンドビルド確認**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit/frontend
npm run build 2>&1 | tail -5
```

期待値: `dist/` 生成完了、エラーなし

---

## Task 5: wails dev 起動・macOS IME 実機確認・コミット

**Files:** なし（確認作業のみ）

- [ ] **Step 1: wails dev を起動**

ユーザーが実行:
```bash
export PATH="$PATH:$HOME/go/bin"
wails dev
```

期待値: アプリウィンドウが開き、上部に `composing: false` インジケータが表示される

- [ ] **Step 2: macOS IME 確認チェックリスト**

以下を手動で確認してください:

**① ひらがな → 漢字変換が正常に確定されるか**
1. エディタをクリックしてフォーカス
2. 日本語入力モードに切り替え（例: `かな` キー）
3. 「にほんご」と入力
4. スペースキーで「日本語」に変換候補を表示
5. Enterキーで確定
- 確認: 確定後にテキストが正しく入力されている ✓/✗

**② 変換中にカーソルが飛ばないか**
1. 長めの文字列を変換中（確定前）に矢印キーを押す
2. カーソルが変換ウィンドウ内に留まっている
- 確認: カーソルが突然エディタ冒頭等に飛ばない ✓/✗

**③ 変換確定後に docChanged が正しく発火するか**
1. 日本語を入力・変換確定
2. ブラウザコンソールを開く（wails dev では `Cmd+Shift+I`）
3. 確定後 800ms 経過後に `[autosave]` ログが出力される
- 確認: `[autosave] section=m0-debug chars=XX` が表示される ✓/✗

**④ IME インジケータが正常に動作するか**
1. 変換開始時にインジケータが黄色に変わる
2. 変換確定後にインジケータが `composing: false` に戻る
- 確認: インジケータが compositionstart/end と連動している ✓/✗

- [ ] **Step 3: 結果の判定**

全て ✓ → M0.5 完了。M1 に進む。

いずれか ✗ → `docs/superpowers/m0-implementation-log.md` に問題を記録し、エディタコアの選定見直しを検討する（STEP.md M0.5 の判定ゲート）。

- [ ] **Step 4: M0.5 完了コミット（確認後に実行）**

```bash
cd /Users/nakamuranatsu/Desktop/develop/tool/micho-edit
git add frontend/src/editor/autosave.ts \
        frontend/src/editor/live-preview.ts \
        frontend/src/editor/setup.ts \
        frontend/src/App.tsx
git commit -m "$(cat <<'EOF'
feat: M0.5 IME 検証 — composing ガード付き autosave/live-preview スタブ実装

- autosave.ts: view.composing ガード + 800ms debounce / 5s flush（M0.5はconsole.logのみ）
- live-preview.ts: composing ガード付きスケルトン ViewPlugin（M0.5は装飾なし）
- setup.ts: sectionId 引数追加、autosave と livePreview を組み込み
- App.tsx: IMEデバッグインジケータ追加（compositionstart/end でバッジ表示）

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

---

## Self-Review チェック

### Spec coverage
- [x] `autosave.ts` の `view.composing` ガード実装 → Task 1
- [x] `live-preview.ts` の `u.view.composing` ガード実装 → Task 2
- [x] macOS (WKWebView) で日本語入力を実機確認（①②③④） → Task 5
- [x] 変換確定後に `docChanged` が正しく発火するか → Task 5 Step 2 ③
- [ ] Windows (WebView2) で同上を確認 → **スコープ外（Windows 環境なし時はスキップ）**
- [x] 問題があればエディタコア選定を見直す → Task 5 Step 3 の判定ゲート

### 注意事項
- `createEditorView` のシグネチャ変更（sectionId 追加）を App.tsx 側でも必ず反映すること（Task 3 Step 2）
- `live-preview.ts` の `_view` のアンダースコアは「使わないが型のために必要」という意味。削除すると TypeScript 型エラーになる
- `autosave.ts` は M1 で `SaveSection` を呼ぶ実装に差し替える。M0.5 では console.log が意図的な実装
